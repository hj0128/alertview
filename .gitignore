__pycache__/
*.pyc
.env
# .env.bak / .env.local 등 사본도 전부 차단(예시 파일만 예외)
.env.*
!.env.example
*.db
*.db-wal
*.db-shm
.venv/
.idea/

# 미블 네이버 세션 프로필(민감 - 절대 커밋 금지)
tools/.mrblog_profile/

tools/mrblog_refresh.log
